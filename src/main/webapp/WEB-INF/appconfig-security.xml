<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
             http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/"  access="permitAll()"/>
        <intercept-url pattern="/login"  access="permitAll()"/>
        <intercept-url pattern="/signup"  access="permitAll()"/>
        <intercept-url pattern="/forgotPassword"  access="permitAll()"/>
        <intercept-url pattern="/resetPassword"  access="permitAll()"/>
        <intercept-url pattern="/resources/**"  access="permitAll()"/>
        <intercept-url pattern="/{username}/*"  access="hasAnyAuthority({'USER', 'ADMIN'}) and #username == principal.username"/>
        <intercept-url pattern="/*/settings/*"  access="hasAnyAuthority({'USER', 'ADMIN'})"/>
        <intercept-url pattern="/**"  access="hasAuthority('ADMIN')"/>

        <form-login login-page="/login"
                    login-processing-url="/login"
                    authentication-success-handler-ref="authenticationSuccessHandler"
                    username-parameter="username"
                    password-parameter="password"/>
        <remember-me key="uniqueAndSecret"/>

        <logout logout-success-url="/login?logout" delete-cookies="JSESSIONID"/>

        <access-denied-handler error-page="/accessDenied"/>

        <csrf disabled="true" />
    </http>

    <beans:bean id="authenticationSuccessHandler" class="todolist.security.AuthenticationSuccessHandlerImpl" />

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref="userDetailsService">
            <password-encoder ref="bCryptPasswordEncoder"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="userDetailsService" class="todolist.services.UserDetailsServiceImpl"/>

    <beans:bean id="bCryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
</beans:beans>